import { Inter } from "next/font/google";

export const inter = Inter({ weight: "400", subsets: ["latin"] });

export const interSemiBold = Inter({ weight: "500", subsets: ["latin"] });

export const interBold = Inter({ weight: "700", subsets: ["latin"] });
